const ProductInfo = (props) => {
  const {content} = props;
  console.log(content);
  return (
    <div className='productInfo'>
      <p></p>
      <p></p>
    </div>
  )
}

export default ProductInfo;