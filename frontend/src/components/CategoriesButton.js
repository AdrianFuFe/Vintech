import { Link } from "react-router-dom";

const CategoriesButton = (props) => {
  return (
    <Link to="/">
      <button id="categories-button"></button>
    </Link>
  );
};

export default CategoriesButton;
