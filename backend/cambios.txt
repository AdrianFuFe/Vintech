CreateUser
    Si daba error al crear el mail el usuario ya estaba en la base de datos, por lo que nunca podria darse de alta con ese error, ya que el email ya existe.
    Abreviado error de que faltan datos necesarios
    res.send
    query introducir datos en db
    vintech place por Vintage jajajjaja
    no introduzco fecha, no es necesario, es automatico al crear usuario
    añadidos paréntesis a connection.release();
    eliminado try catch de la query (simplemente por estilo)

InitDb
    status users: eliminado deleted y añadido inactive
    products: ON DELETE CASCADE
    product_imgs: ON DELETE CASCADE
    eliminado pending de bookings y messages
    status:deleted en products eliminado

helpers:
    variable to: email renombrado y desestructurado objeto
    infoPicture por infoImage

recoverPwd:
    eliminados algunos try catch por estilo

validAuth:
    en query cambio de variable id por tokenInfo.id
    cambio de todo ese código por entryExists en helpers

getUser:
    antes obtenía toda la info, incluida contraseña. Query modificada.
    ahora también obtiene sus productos asociados.
    modificado res.send

editUser:
    cambio de req.files.picture por req.files.img
    cambio de directory avatares por avatars

changePwd:
    modificado el nombre del fichero para ser coherente con la función
    solo obtengo pwd de la base de datos por seguridad
    introducido newPwd en la base de datos, no el old, que nunca cambiaba la contraseña
    el bcrypt lanza error si las contraseñas no coinciden, entonces cambio el catch por un next

resetPwd:
    cambios menores

canEdit:
    'creado'

server.js:
    edit password por change password