import { Link } from "react-router-dom";

const MessagesButton = (props) => {
  return (
    <Link to="/">
      <button id="messages-button"></button>
    </Link>
  );
};

export default MessagesButton;
